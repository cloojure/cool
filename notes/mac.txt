
xcode-select --install      # apple dev tools

software to load:
  brew      <== do this first!!!

install:
  Chrome
  IntelliJ IDEA
  Slack
  HomeBrew (aka "brew")

brew install:
  bash      (newest version)
  dos2unix
  git
  h2
  iterm2 (shows up at "iterm" replacing default "iterm" from apple)
  macvim
  pgadmin3
  postgresql
  rlwrap
  tcsh
  tree
  vim
  zsh

web download & install:
  chrome
  squirrel (sql client)
  hipchat
  skype
  java
  IntelliJ Idea (JetBrains.com)
  libreoffice (or brew?)

git clone:
  cool.git
    cd ~/cool  <- install dir
    ./install.zsh


Apple Menue (top left of screen)
  -> About This Mac
    -> Software Update
