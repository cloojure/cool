
software to load:
  homebrew      <== do this first!!!  (brew.sh)

install:
  Chrome
  HomeBrew (aka "brew")

brew install:   (see brew --help, brew search XXX, brew install XXX)
  bash      (newest version)
  babashka  -> ~/opt
  zprint    -> ~/opt/zprint-filter-1.1.1 (jar file)
  dos2unix - brew install dos2unix
  git  - brew install git
  gitk
  htop
  iterm2 - iterm2.com (named "iterm" in Applications folder)
  macvim  - brew install macvim 
  ctags - exuberant ctags
  openjdk - brew install openjdk
    brew install java11
  rlwrap - brew install rlwrap
  tree - brew install tree
  libreoffice
    brew install  homebrew/cask/libreoffice
  postgresql pgadmin3
  python2, python3, python-pip, python3-pip, pylint, virtualenv
  curl 
  gcc, ruby, maven, octave, gimp
  s3cmd, iotop, gnupg, youtube-dl, ctags
  zprint:  # not included in ~/cool/bin
    brew install  --cask zprint  ; ***** fails on Looking Glass mac *****
  zsh - if not already installed (i.e. older OSX)

web download & install:
  squirrel (sql client)
  java
    oracle.com/java
  IntelliJ Idea (JetBrains.com)
  slack
  docker
  dropbox

git clone:
  cool.git
    cd ~/cool  <- install dir
    ./install.zsh --force

Apple Menue (top left of screen)
  -> About This Mac
    -> Software Update

Optional(?)
  xcode-select --install      # apple dev tools
  h2

