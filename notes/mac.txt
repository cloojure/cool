
2025-08-06 macbook air setup

allow/setup keyboard
mac mouse disable "natural scrolling"

install:
  Chrome

HomeBrew (aka "brew")
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

brew install macvim iterm2
brew install git
brew install git-gui

git clone
  mkdir dotfiles-local
  Bitbucket:
    catalan42/alan (thomnpson2526@gmail.com) 
  GitHub
    io-tupelo/dotfiles
    cloojure/cool
    cloojure/tupelo

brew install clojure/tools/clojure
brew install python pylint virtualenv
brew install curl gcc ruby maven octave gimp  s3cmd
brew install borkdude/brew/babashka
brew install bash dos2unix htop ctags rlwrap tree curl 
brew install homebrew/cask/libreoffice

Maybe?
  brew install gnupg

  postgresql pgadmin3
  brew install  --cask zprint  ; ***** fails on Looking Glass mac *****
  zsh - if not already installed (i.e. older OSX)
  youtube-dl    # no longer maintained....?

web download & install:
  squirrel (sql client)

  sdkman
    curl -s "https://get.sdkman.io" | bash   # then New Window
    sdk  install  java 24.0.2-oracle
  
  JetBrains.com
    IntelliJ 
    PyCharm
  slack
  docker desktop
  dropbox

git clone:
  cool.git
    cd ~/cool  <- install dir
    ./install.zsh --force

Apple Menue (top left of screen)
  -> About This Mac
    -> Software Update

Optional(?)
  xcode-select --install      # apple dev tools
  h2

