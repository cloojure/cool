
software to load:
  brew      <== do this first!!!  (brew.sh)

install:
  Chrome
  IntelliJ IDEA
  Slack
  HomeBrew (aka "brew")

brew install:
  bash      (newest version)
  dos2unix
  git  - brew install git
  h2
  iterm2 - iterm2.com (named "iterm" in Applications folder)
  macvim
  pgadmin3
  postgresql
  rlwrap
  tcsh
  tree
  vim
  zsh

web download & install:
  squirrel (sql client)
  hipchat
  skype
  java
  IntelliJ Idea (JetBrains.com)
  libreoffice (or brew?)

git clone:
  cool.git
    cd ~/cool  <- install dir
    ./install.zsh


Apple Menue (top left of screen)
  -> About This Mac
    -> Software Update

Optional(?)
  xcode-select --install      # apple dev tools

